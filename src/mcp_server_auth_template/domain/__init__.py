"""Business entities, Value Objects, invariants, and domain errors.

Framework-free by the Clean Architecture dependency rule (`docs/ARCHITECTURE.md`,
`docs/adr/0001-clean-architecture.md`): no imports from `application`, `adapters`, or
`entrypoints`, and no third-party SDKs.
"""
